# MarkovPortfolio V4 의존성
# pip install -r requirements.txt 로 설치하세요.
#
# 버전 고정 정책:
# - >= 를 사용하여 최소 버전을 보장하되 패치 업데이트를 허용합니다.
# - 주요 버전 변경(예: pydantic v1 → v2)은 코드 수정이 필요하므로 주의합니다.

# AI API — Anthropic Claude (claude-sonnet-4-6 사용)
anthropic>=0.40.0

# 웹 프레임워크
fastapi>=0.115.0
uvicorn[standard]>=0.32.0      # standard: websockets, httptools 포함

# 데이터 검증 — Pydantic v2 필수 (.model_dump() 사용, .dict() 금지)
pydantic>=2.9.0

# 환경변수 로딩 — .env 파일에서 API 키를 읽기 위해 필요합니다.
python-dotenv>=1.0.0

# 시장 데이터 — yfinance 1.2.0+ MultiIndex 처리 필요
# df.columns = df.columns.droplevel(1) 패턴으로 처리합니다.
yfinance>=0.2.50

# 수치 계산 — VaR, 변동성, 베타 계산에 사용합니다.
numpy>=2.1.0

# HTTP 클라이언트 — FastAPI 테스트 및 외부 API 호출에 사용합니다.
httpx>=0.27.0

# 통계 계산 — 정규분포 기반 VaR 계산에 사용합니다.
scipy>=1.14.0
